# FastAPI and server dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# HTTP client for video downloads
requests>=2.31.0

# Data validation and settings
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Note: PyTorch is installed separately in Dockerfile with CUDA support:
# pip install torch==2.7.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126

# SAM 3 runtime dependencies (pulled in by sam3 but ensure present)
einops>=0.7.0
decord>=0.6.0
pycocotools>=2.0.0
psutil>=5.9.0
opencv-python-headless>=4.8.0

# Note: SAM 3 is installed from source in Dockerfile:
# git clone https://github.com/facebookresearch/sam3.git
# pip install .
